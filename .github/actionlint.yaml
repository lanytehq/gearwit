---
# actionlint configuration.
#
# These labels are deployed custom runner labels. This file only teaches
# actionlint about them; it does not provision runners.
self-hosted-runner:
  labels:
    - ubuntu-latest-arm64-s
    - windows-latest-arm64-s
